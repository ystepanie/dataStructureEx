package Queue;

import LinkedList.LinkedListNode;

public class Queue {
	LinkedListNode front;
	LinkedListNode back;

	public Queue(LinkedListNode front, LinkedListNode back) {
		this.front = front;
		this.back = back;
	}
}
